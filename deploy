find matject -mindepth 1 -maxdepth 1 ! -name "screenshots" -exec rm -rf {} +

pushd matject-src

npm run docs:build

popd

mv matject-src/.vitepress/dist/* matject

git add . 

git commit -m "update /matject" 

git push